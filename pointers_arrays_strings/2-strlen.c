#include "main.h"

/**
 * _strlen - punto de entrada
 * @s: caracter
 * Return: tamaño del string
 */
int _strlen(char *s)
{
	int lent;

	for (lent = 0; s[lent] != '\0'; lent++)
	{
	}
	return (lent);
}
