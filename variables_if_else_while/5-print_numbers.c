#include <stdio.h>

/**
 * main - punto de entrada
 * Return: 0
 */

int main(void)
{
	int decimal = 0;

	while (decimal < 10)
	{
		printf("%d", decimal);
		decimal++;
	}
	putchar('\n');
	return (0);
}
